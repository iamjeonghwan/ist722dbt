name: 'fudgemart_dw'
version: '1.0.0'

profile: 'fudgemart_dw'

model-paths: ["models"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  fudgemart_dw:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: fudgecompanies
